version: 2.1

orbs:
 android: circleci/android@0.2.0
 
jobs:
  build:
    executor: android/android
#         /app/build/outputs/apk/deviceForTesters/release/app-deviceForTesters-release.apk
    steps:
      - checkout
      - run:
          name: file check before
          command: ls -l
#       - run:
#           command: chmod +x ./gradlew
#       - run:
#           command: ./gradlew build
      - run:
          name: curl
          command: curl --header 'Authorization: token $githubtoken' --header 'Accept: application/vnd.github.v3.raw' --remote-name --location "https://api.github.com/repos/machinne/test-key/contents/testcovid"
#       - run:
#           command: |
#             cd ./app/build/outputs/apk/deviceForTesters/release
#             ls -l
      - run:
          name: file check after
          command: ls -l
            
